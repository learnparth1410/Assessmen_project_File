# 4. For a Pandas Data Frame created from a NumPy array, what is the default behavior for
# the labels for the columns? For the rows? 

#ANS:When you create a Pandas DataFrame from a NumPy array, the default behavior is to use integer labels for both columns and rows. Columns are labeled with integers starting from 0, and rows are also labeled with integers starting from 0.